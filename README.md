# ContpaqSDK-API

This is the solution for the implementations that require the Contpaqi Comercial sdk, the sdk is consumed by an api (that is prepared to be a windows service) and well the different presentation layers consume the same core layers
